<?xml version="1.0" encoding="utf-8"?>
<!-- Main -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/login"
    android:clipToOutline="true">

    <!-- s -->
    <!-- engranaje 1 -->

    <!-- user (1) 1 -->

    <!-- Ellipse 1 -->


    <ImageView
        android:id="@+id/ivRoundLogo"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginLeft="56dp"
        android:layout_marginEnd="56dp"
        android:background="@mipmap/roundlogo"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.359" />

    <ImageButton
        android:id="@+id/btnAjustes"
        android:layout_width="73dp"
        android:layout_height="76dp"
        android:layout_marginLeft="52dp"
        android:layout_marginTop="16dp"
        android:background="@mipmap/ajustes"
        android:foreground="@mipmap/ajustes"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="SpeakableTextPresentCheck" />

    <Button
        android:id="@+id/btnPerfil"
        android:layout_width="76dp"
        android:layout_height="76dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="56dp"
        android:background="@mipmap/user"
        android:foreground="@mipmap/user"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="SpeakableTextPresentCheck" />

    <Button
        android:id="@+id/btnParty"
        android:layout_width="167dp"
        android:layout_height="67dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="76dp"
        android:backgroundTint="@color/azul_oscuro"
        android:text="PARTY"
        app:cornerRadius="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ivRoundLogo"
        app:layout_constraintVertical_bias="1.0" />

    <Button
        android:id="@+id/btnBattle"
        android:layout_width="167dp"
        android:layout_height="67dp"
        android:layout_marginLeft="16dp"
        android:layout_marginBottom="76dp"
        android:backgroundTint="@color/azul_oscuro"
        android:text="BATTLE"
        app:cornerRadius="10dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ivRoundLogo"
        app:layout_constraintVertical_bias="1.0" />

</androidx.constraintlayout.widget.ConstraintLayout>